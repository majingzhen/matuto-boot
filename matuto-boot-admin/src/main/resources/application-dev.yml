# Spring
spring:
  # datasource
  datasource:
    druid:
      master:
        url: jdbc:mysql://localhost:3306/matuto_boot?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: root
        password: 123456
  # redis
  data:
    redis:
      host: localhost
      port: 6379
      database: 1

# Logging
logging:
  level:
    com.matuto.boot: debug
    org.springframework: warn

